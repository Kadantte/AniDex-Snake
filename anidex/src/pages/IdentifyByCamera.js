import React from 'react';
import CameraCapture from '../components/CameraCapture';

const IdentifyByCamera = () => {
    return (
        <div>
            <h2>Identify Snake by Camera</h2>
            <CameraCapture />
        </div>
    );
};

export default IdentifyByCamera;
